"""Vendored rules pack (read-only data; do not import as Python).

This directory contains YAML rule definitions vendored from
``harrydaihaolin/agent-readiness-rules``. It is treated as a Python
package only so ``importlib.resources.files()`` can locate it inside an
installed wheel; nothing here is meant to be imported.
"""
